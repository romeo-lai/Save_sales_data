# Save_sales_data
Now the sales data are stored by date, separated in multiple dbf files in multiple directories, this project is to read the table and transcript the sales data of each day and integrate them into one local temp sql database for data ana.
